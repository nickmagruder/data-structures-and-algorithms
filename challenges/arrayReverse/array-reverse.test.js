/* 'use strict';

const arrayReverse = require('..')

describe('Testing the array-reverse module', () => {
    it('should return an aray with reversed elements', () => {
        const reversedArray = arrayReverse([]);

        expect(reversedArray).toBe([])
    })
}) */